<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dxc.mycollector">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".taskDownload.DLApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BaseActivity" />
        <activity android:name=".RegisterAcitvity" />
        <activity android:name=".PersonAcitvity" />
        <activity android:name=".TaskDownloadActivity" />
        <activity android:name=".BlueToothFolder" />
        <activity android:name=".ShowExamineRecord" />
        <activity android:name=".AddExamineRecord" />

        <service android:name=".taskDownload.DownLoadService" />

        <activity android:name=".CeLiangActivity" />
        <activity android:name=".SafeUpdateActivity"></activity>
    </application>

</manifest>